import Game from "../src/models/game";

describe('A game', () => {
    let testGame;

    beforeAll(() => {
        testGame = new Game({
            up: 'idk',
            down: 'idk'
        });
    });

    it('get the turn', () => {
        
    })
});